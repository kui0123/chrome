# Mark2AI - Chrome 扩展

> **当前版本: v2.0.0**

一个用于在浏览器中标记 HTML 组件、**可视化编辑 CSS 样式和 HTML 内容**，并生成 diff 文件供 AI Agent 迭代使用的 Chrome 扩展。

## 使用方法

### 快速上手

1. 打开任意网页（`http://` 或 `https://` 页面）
2. **唤醒工具栏**（三态循环切换：隐藏 → 唤醒按钮 → 完整工具栏 → 隐藏）
**方式 A**：点击浏览器工具栏中的扩展图标切换
**方式 B**：使用快捷键 `Alt+E`（macOS 为 `⌥+E`）切换
3. 开始选择模式：
**方式 A**：快捷键`Alt+"+"`（macOS 为 `⌥+"+"`）进入选择模式
**方式 B**：右键点击 → 在工具栏「选择」按钮 "开始/停止 选择元素"
4. **标记组件**：在页面上点击需要标记的组件（组件会以主题色虚线高亮）
5. **多选标记**：按住 `Shift` 点击多个元素（主题色虚线高亮），然后点击「组合标记」按钮生成组合标记
7. 标记后自动弹出编辑面板，你可以：
   - **直接拖拽**：鼠标拖动元素改变位置
   - **调整大小**：拖动元素边缘的把手改变尺寸
   - **滚轮缩放**：按住 `Ctrl/Cmd + 滚轮` 缩放元素大小
   - **双击编辑**：双击元素直接编辑 HTML 内容（光标定位在点击位置）
   - **精确编辑**：点击元素上的 `#N` 编号徽章打开检查面板
   - **删除当前元素**：点击删除角标删除当前元素
   - **Diff 导出**：一键生成包含原始 HTML、修改后 HTML 和 CSS 修改的完整 Markdown 与 JSON 文件

### 检查面板详解

点击标记元素上的 `#N` 编号徽章，弹出编辑面板包含以下区域：

| 区域                     | 功能                                                                            |
| ---------------------- | ----------------------------------------------------------------------------- |
| **元素信息**               | 显示 CSS 选择器，供开发参考                                                              |
| **组件标签**               | 简短文字备注，方便快速识别组件用途                                                             |
| **修改说明（给 AI Agent 看）** | 多行文本描述，详细说明本次修改的目的、设计意图，供 AI Agent 理解并应用相同风格                                  |
| **位置调整**               | 左偏移 (left) / 上偏移 (top) 的滑块精确调整（-500 \~ +500px，步长 1px），右侧数值可点击直接编辑             |
| **大小调整**               | 宽度/高度的滑块精确调整（10 \~ 2000px，步长 1px），右侧数值可点击直接编辑；支持 px / % 单位切换                  |
| **样式编辑**               | 颜色、背景色、背景图片、边框、圆角、字体大小等属性；支持上传本地图片作为背景，背景图片编辑区内显示背景预览，样式分组顶部提供「重置」按钮，可恢复该分组样式 |
| **HTML 编辑**            | 原始 HTML（只读）和修改后的 HTML                                                         |
| **底部操作**               | 删除此标记 / 保存修改（面板位置、单元素编辑面板尺寸和折叠状态会在当前标签页内自动记住）                                 |

### 自定义字体使用说明

**添加自定义字体**：

1. 在检查面板的「字体」下拉框下方，点击「+ 添加自定义字体」按钮
2. 输入字体显示名称（如："我最喜欢的字体"）
3. 输入 CSS font-family 值（如：`"MyFont", sans-serif`）
4. 点击确定后，自定义字体将出现在下拉框的「自定义字体」分组中

**删除自定义字体**：

1. 在下拉框中选择要删除的自定义字体
2. 点击字体选择器旁的「−」按钮
3. 该字体将从自定义字体列表中移除；如果当前元素正在使用该字体，会同步清理当前元素的字体样式；其他标记中已保存的同名字体值会保留并提示

**字体预览提示（三态）**：

- **失败态（黄色警告）**：当前选择的字体在系统上不可用时显示（包括预设字体和自定义字体）
- **引导态（蓝色信息）**：无自定义字体且未选择字体时的操作引导提示
- **成功态（绿色成功）**：当前选择的自定义字体预览正常时的确认提示

**注意**：

- 自定义字体保存在浏览器本地存储中，清除浏览器数据会导致自定义字体丢失
- 添加的字体在 Diff 中会按用户输入的 font-family 值保存，AI 将依照选项修改

### 工具栏按钮详解

工具栏布局：顶部一排 4 个圆角矩形按钮 + 中间大导出按钮 + 底部图标按钮行

**顶部一排 —— 4 个圆角矩形按钮**

- **选择**：进入/退出选择模式（点击页面元素进行标记）
- **复制**：复制当前编辑元素的 DOM 并作为新的标记组件
- **新增**：弹窗输入 HTML 代码，在当前编辑元素后插入新组件
- **删除**：从 DOM 中完全删除当前编辑的元素及其标记

**中部 —— 导出按钮**

- **导出 Diff**：下载 Markdown 和 JSON 格式的修改报告

**底部 —— 图标按钮行**

- **设置**：打开设置面板，控制编号徽章、拖拽把手、快捷键提示的显示开关
- **清空所有**：清除所有已标记的组件

**计数行**：显示当前标记总数、已修改数（带编辑内容或样式改动的组件）

### 工具栏状态

- **隐藏态**：页面上无任何 UI（初始状态）
- **唤醒态**：页面显示一个小圆按钮（点击展开工具栏）
- **激活态**：完整工具栏显示，可操作

切换方式：点击浏览器扩展图标 **或** 使用快捷键 `Alt+E`（macOS `⌥+E`）三态循环切换；`Alt+"+"`（macOS `⌥+"+"`）在扩展已注入页面后快速进入选择模式

### 对齐辅助线详解

当你拖动元素位置或调整大小时，扩展会自动检测与其他标记元素的对齐关系：

- **大小对齐**：当前元素的宽度/高度与其他标记元素相差 ≤ 5px 时
  - 显示红色垂直线（左右边界）/水平线（上下边界）
  - 尺寸浮窗显示 "↔ 200 对齐!" 提示
- **位置对齐**：当前元素的 left/center/right 或 top/center/bottom 与其他元素接近时
  - 显示贯穿整个页面的红色垂直线/水平线
  - 位置浮窗显示 "对齐!" 提示

辅助线帮助你快速将多个元素对齐到统一尺寸或位置，非常适合做设计稿还原。

### 导出 Diff

点击工具栏中的 **导出 Diff** 按钮，下载两个文件：

- `html-diff-<时间戳>.md` - Markdown 格式报告，适合 AI Agent 阅读
- `html-diff-<时间戳>.json` - 结构化数据，便于程序化处理

## 导出文件格式

### Markdown 文件（`.md`）

包含内容：

- 给AI的指令
- 页面信息（URL、标题、时间戳）
- 每个组件的原始 HTML 和修改后 HTML
- CSS 样式修改记录（属性名 + 新值）
- 行级 diff（`-` 表示删除，`+` 表示新增）
- 给 AI Agent 的修改说明
- 完整的 JSON 数据（便于程序化处理）

### JSON 文件（`.json`）

结构化数据，每个标记组件包含：

- `index` - 编号
- `tag` - 元素标签
- `selector` - CSS 选择器
- `originalHTML` - 原始 HTML
- `modifiedHTML` - 修改后的 HTML
- `originalStyles` - 原始样式快照
- `modifiedStyles` - 修改的样式属性字典
- `note` - 组件标签备注
- `description` - 修改说明
- `hasChange` - 是否有修改

## 使用 Diff 给 AI Agent

将生成的 `.md` 文件内容直接复制粘贴到对话中，或直接将文件作为附件上传。

```
请根据[在此粘贴 diff .md 文件地址]对[在此粘贴待修改文件地址]执行修改。
```
## 功能特性（版本记录）

### 核心功能

- **组件选择与标记**：点击页面任意 HTML 元素进行标记，显示彩色外框与编号徽章（已选中为绿色、已修改为橙色、多选为主题色虚线）
- **可视化编辑**：拖拽移动、调整大小、滚轮缩放元素大小
- **CSS 样式编辑**：在检查面板中直接编辑位置、尺寸、颜色、边框、圆角等属性
- **HTML 内容编辑**：双击标记的元素可直接编辑其 HTML 文本内容（光标定位在点击位置，不再全选文本）
- **链接地址编辑**：对 `<a>` 标签元素可直接在检查面板中修改 `href` 属性，编辑时不会跳转
- **单个标记删除**：每个标记元素左上角有红色删除角标，点击即可删除该标记
- **实时预览**：所有样式修改立即反映在页面上
- **对齐辅助线**：调整大小和位置时自动显示红色对齐辅助线和尺寸/坐标提示
- **复制当前元素**：在工具栏中可一键复制当前编辑元素的 DOM 并标记为新组件
- **添加新元素**：通过工具栏可直接通过 HTML 代码动态添加新的组件
- **删除当前元素**：从 DOM 中完全移除当前编辑元素
- **Diff 导出**：一键生成包含原始 HTML、修改后 HTML 和 CSS 修改的完整 Markdown 与 JSON 文件
- **快捷键支持**：`Alt+E`（Windows/Linux）/ `⌥+E`（macOS）三态循环切换；`Alt+"+"`（Windows/Linux）/ `⌥+"+"`（macOS）在扩展已注入页面后快速进入选择模式

### v1.1.0 新增功能

- **字体选择**：支持 15 种预设字体（中文 10 种 + 英文 5 种），覆盖商务、互联网常用风格
- **字体粗细**：6 档字体粗细可选（300 细体 \~ 800 特粗），并支持默认值
- **响应式单位**：大小调整支持 px / % 单位切换，百分比模式下自动适配不同屏幕尺寸
- **编辑面板最小化**：点击 Header 的「−」按钮可折叠面板，只保留标题栏
- **面板大小拖拽**：右下角拖拽把手可自由调整编辑面板尺寸（最小 300×200px）
- **面板尺寸记忆**：调整后的面板尺寸会自动记住，下次打开保持一致
- **DOM 变更持久化**：复制、删除、添加元素的操作在刷新后仍然保留（sessionStorage 持久化）

### v1.3.0 新增功能

- **Shift+多选 + 组合标记**：进入选择模式后，按住 Shift 点击可多选元素，生成组合标记后可整体拖拽
- **组合标记编辑面板**：点击组合标记的 G 徽章打开面板，支持整体缩放滑块、子元素列表、解散组合、删除组合
- **组合标记持久化**：组合标记在刷新页面后自动恢复

### v1.5.0 新增功能

- **背景图片编辑区垂直串行布局**：背景图片预览图与信息按钮改为上下排列，布局更清晰
- **字体预览提示增强为三态**：失败态（黄色警告，字体不可用）、引导态（蓝色信息，无自定义字体）、成功态（绿色成功，字体正常）
- **重置按钮优化**：样式分组顶部提供「重置」按钮，可恢复该分组样式
- **多选元素支持复制/删除**：多选模式下可批量复制或删除选中元素；删除会真正移除选中元素对应 DOM，而不是仅清除标记
- **删除标记后页面排布错乱修复**：修复删除标记后页面元素布局错乱的问题
- **自定义字体弹窗/图片大小提示弹窗按钮可点击修复**：修复弹窗按钮无法点击的交互问题
- **滚轮缩放失效修复**：修复 Ctrl/Cmd + 滚轮缩放功能在部分场景下失效的问题

### v1.6.0 新增功能

- **UI 主题全面升级为「暮紫」设计语言**：整体视觉风格统一为紫色渐变主题，界面更精致
- **检查面板完整重设计**：10 个编辑分区（元素信息/组件标签/修改说明/位置调整/大小调整/样式编辑/字体编辑/背景编辑/边框圆角/HTML 编辑），布局更清晰
- **工具栏全新 UI**：渐变头部 + 图标按钮网格 + 状态计数，视觉层次更分明
- **新增多选工具栏**：多选元素后浮动显示组合标记/复制/删除/取消选择四个操作按钮
- **Toast 通知支持 4 种类型**：info/success/warning/error 四种通知样式，自带图标和渐变背景
- **模态弹窗样式升级**：毛玻璃遮罩 + 渐变头部 + 入场动画，交互体验更流畅
- **全套 CSS 变量设计令牌体系**：统一的颜色/阴影/圆角/间距/字体变量，便于后续主题切换

### v1.7.0 新增功能

- **UI 全面升级为 v4 精细化设计**：位置/大小调整改为滑块交互，操作更直觉
- **工具栏全新布局**：一排 4 个圆角按钮 + 大导出按钮 + 底部图标按钮
- **设置面板升级**：新增 iOS 风格开关组件，支持显示编号徽章、拖拽把手、快捷键提示等开关控制（全局生效，保存至 chrome.storage.local）
- **全面去 Emoji 化**：统一使用 Unicode 符号，视觉更专业
- **架构级修复**：编号徽章和拖拽把手采用 body 层级浮层方案，彻底解决 overflow:hidden 裁剪问题
- **嵌套滚动兼容**：支持任意嵌套层级的滚动容器内标记元素，浮层始终正确跟随
- **Bug 修复**：修复编辑面板无法折叠的问题
- **功能优化**：移除同步缩放子元素功能，简化操作流程
- **体验优化**：新增双击元素打开编辑面板，徽章隐藏时仍可操作
- **健壮性增强**：浮层高 z-index 防遮挡、元素被外部删除时自动清理、设置异步加载兜底

### v2.0.0 新增功能

- **V5 全新 UI 设计**：暮紫主题 + Mac 风格工具栏 + 全滑块编辑面板
- **四套预设主题**：暮紫/深藏青/灰绿/暖棕 + 自定义颜色
- **基于 HSL 的本地颜色衍生算法**：黑白边界保护，主题色自动生成和谐配色
- **样式隔离系统**：all: initial + all: unset，避免宿主页面 CSS 冲突
- **Toast 通知系统**：4 种类型（info/success/warning/error），替代原生 alert
- **模态弹窗系统**：替代原生 confirm/prompt，支持确认框和输入框
- **iOS 风格设置面板**：主题切换 + 显示开关（徽章/把手/快捷键提示）
- **字体预览三态提示**：成功/信息/警告，自动检测字体可用性
- **chrome.storage.local 持久化**：标记数据跨页面刷新保留
- **双击元素打开编辑面板**：快速进入精确编辑模式
- **多选工具栏补全**：批量复制/批量删除，多选操作更便捷
- **移除同步缩放子元素功能**：简化操作流程
- **全面去 Emoji 化**：统一使用 SVG 图标，视觉更专业

### 交互方式

- **浮动工具栏**：页面右上角的可拖拽工具栏，显示标记计数与操作按钮（初始位置与唤醒按钮一致，右上角 top: 20px）
- **三态切换**：工具栏完全隐藏 → 唤醒按钮 → 完整工具栏显示，循环切换
- **检查面板**：点击标记元素上的编号徽章，弹出编辑面板用于精确调整样式（面板位置、单元素编辑面板尺寸和折叠状态会在当前标签页内记住）
- **右键菜单**：在页面任意位置右键可快速触发开始/停止选择组件、生成 Diff 文件、清除所有标记
- **键盘快捷键**：`Alt+E`（Windows/Linux）或 `⌥+E`（macOS）—— 三态循环切换；`Alt+"+"`（Windows/Linux）或 `⌥+"+"`（macOS）—— 在扩展已注入页面后快速进入选择模式

### 标记元素上的交互

在每个标记的元素上，你可以直接进行以下操作：

| 操作       | 方式                        | 效果                                   |
| -------- | ------------------------- | ------------------------------------ |
| **移动位置** | 拖动元素本体（避开徽章/把手区域）         | 改变元素相对位置，显示坐标和位置辅助线                  |
| **调整大小** | 拖动边缘主题色圆形把手（8 方向）         | 改变元素宽高，显示尺寸和大小对齐辅助线                  |
| **滚轮缩放** | `Ctrl/Cmd + 滚轮`           | 元素等比缩放                               |
| **编辑文案** | 双击元素                      | 进入文本编辑模式，光标精确定位在点击位置，Enter 保存，Esc 取消 |
| **打开面板** | 点击右上角绿色/橙色编号徽章，或双击元素      | 弹出完整编辑面板（样式/尺寸/位置/HTML/修改说明）         |
| **删除标记** | 点击左上角红色圆形 (x) 按钮          | 删除该标记，恢复原始样式                         |
| **编辑链接** | （`<a>` 标签）在编辑面板修改 href 字段 | 修改链接地址，防止编辑时误跳转                      |

### 组合标记操作

| 操作         | 方式                      | 效果                         |
| ---------- | ----------------------- | -------------------------- |
| **多选元素**   | 选择模式下按住 `Shift` 点击      | 加入/移出多选集（主题色虚线高亮）          |
| **创建组合**   | 多选后点击浮动工具栏「组合标记」        | 生成组合标记，共享一个主题色外框和 G 编号徽章   |
| **整体移动**   | 拖动组合区域                  | 所有子元素同步移动，保持相对位置           |
| **整体调整大小** | 拖动组合边缘把手（8 方向）          | 整体等比缩放                     |
| **组合面板**   | 点击 G 徽章                 | 打开组合编辑面板（缩放滑块/子元素列表/解散/删除） |
| **解散组合**   | 组合面板 → 「解散组合」按钮         | 移除组合外壳，子元素作为独立标记保留         |
| **删除组合**   | 组合面板 → 「删除组合」按钮         | 删除组合及所有子元素标记               |

<br />


## 注意事项

- 扩展仅在 `http://` 和 `https://` 页面生效，不支持 `chrome://` 等内部页面
- 标记数据存储在 `sessionStorage`，刷新页面后标记保留，关闭标签页后丢失
- 建议及时导出 Diff 文件
- 不要在含有敏感信息的页面使用此扩展
- 组件的 CSS 选择器是自动生成的，仅用于参考
- 拖拽元素时确保点击的是元素本体（不是徽章/把手区域）
- 对 `<a>` 链接元素的点击行为已被拦截，编辑时不会跳转到其他页面
- 编辑面板的位置会在当前标签页 sessionStorage 中记住，页面刷新后保持位置不变，关闭标签页后清除
- 单元素编辑面板的尺寸（拖拽调整后）和编辑面板折叠状态也会在当前标签页 sessionStorage 中记住；折叠状态下不会恢复高度尺寸
- 组合编辑面板尺寸沿用当前页面运行时共享的 `state.inspectorSize`，不承诺刷新后从 sessionStorage 恢复尺寸
- 复制、删除、添加元素等 DOM 结构变更会持久化，刷新页面后仍然保留
- 点击「清空所有标记」会恢复所有被删除的 DOM 元素到原位置
- 非链接元素添加跳转链接后，点击会在新标签页打开；编辑模式下点击不会跳转
- 修改说明支持 Markdown 链接格式 `[链接文字](https://...)`，点击「预览内容」可在面板内查看渲染效果
- 部分字体（如幼圆、隶书等）是 Windows 系统自带字体，在 macOS/Linux 上可能不可用，扩展会自动检测并通过三态提示显示字体预览状态（失败态/引导态/成功态）
- 自定义字体保存在浏览器本地存储（chrome.storage.local）中，清除浏览器数据会丢失

## 许可证

MIT License
